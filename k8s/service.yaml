apiVersion: v1
kind: Service
metadata:
  name: jenkins-github-ci
spec:
  type: NodePort
  selector:
    app: jenkins-github-ci
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
